version: 1.0
description: This checks if a action should be preformed and then executes the action if needed.

input:
  - trigger
tasks:
  eval:
    action: sql.query
    input:
      connection: mysql
      query: 'select count(*) FROM executions Where '
